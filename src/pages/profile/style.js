export const containerStyles = {
    margin: '20px',
  };
  
  export const gridContainerStyles = (theme) => ({
    spacing: 2,
    justifyContent: "center",
    alignItems: "center",
    maxWidth: 800,
    marginTop: 5,
    backgroundColor: theme.palette.mode === "dark" ? "#1F2A40" : "#f0f0f0",
    borderRadius: "8px",
    padding: 3,
  });
  
  export const avatarStyles = {
    width: "100%",
    height: "auto",
  };
  
  export const cardStyles = (theme) => ({
    backgroundColor: theme.palette.mode === "dark" ? "#2E3B55" : "white",
  });
  
  export const tableCellLabelStyles = {
    width: '30%',
  };
  
  export const tableCellValueStyles = {
    width: '70%',
  };
  
  export const buttonStyles = {
    marginTop: 2,
  };
  